# Python dependencies are added per stage.

# Stage 1 — ingestion from the openFDA API
requests
python-dotenv

# Stage 2 — loading raw JSON into a local DuckDB warehouse
duckdb

# Stage 3 — transformations with dbt (DuckDB adapter)
dbt-core
dbt-duckdb

# Stage 6 — orchestration
prefect
